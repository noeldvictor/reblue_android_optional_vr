# Shared native water visual ownership

2026-09-08. Source3102f19 plus this connected change; Vulkan desktop only.
This is visual/material ownership progress, not a completed host frame or new
game-pixel, HDR, stereo or Quest qualification.

## Connection and source contracts

The sorted consumer now includes known water identities in the existing late
visual publication, even in a water-only list. Water and ordinary native objects
share `NativeDeferredVisualScope`: primary receiver, diffuse-class compatibility
exports, native blend intent and paired cleanup. No water visual callback or
translated visual shader setup executes on that admitted route. The ordinary
rigid model contract stays type0/no-op-resource-only; water is not mislabeled as
an ordinary rigid model.

The existing instance source index supplies copied identity/frame/generation
checked values. After receiver compatibility writes, the current visual is
reimported at the producer before shader class/blend use; this avoids freezing a
possible late alias and avoids republishing all requested visuals at every
transition. Completed water writers still refresh the whole active publication
for cross-visual aliases. No second registry, queue or source-keyed native asset
format was introduced.

The completed water material output now owns object inputs too. Its native
consumer composes material features from that copy, not another visual-field
lookup. Image capture is explicit in the direct producer. The now-unused
thread-local callback-capture pointer, destructor cleanup and legacy hook
publication branch were removed. Single ordered light commit, image leases,
late snapshot selection and outgoing compatibility exports remain intact.

Source map, reusing the previous material/receiver audit:

- `config/hooks/render_list.toml`: sorted main-view visual begin supplies extra1;
  legacy replay cannot own a visual transition. No hook/codegen inputs changed.
- `sub_82174648`, `generated/reblue_recomp.88.cpp:3726`: extra1 jumps to
  0x821749E4, then reads visual+3132 for diffuse-class zero/restore. Only visual
  types1/11 add fur parameter work after that branch. Extra0's material/phase
  behavior is not borrowed for this route.
- `sub_82177650`, `generated/reblue_recomp.71.cpp:3728`: indexed receiver effects
  require type14. The existing primary producer's phase3 participation is the
  same for all other admitted types. `rex_DebugPrint` at0x820D1998,
  `generated/reblue_recomp.41.cpp:119`, simply returns1 for the auxiliary slot.
- `sub_82174C60`, `generated/reblue_recomp.24.cpp:3931`: existing late scalar
  diffuse restore implementation is reused, including scalar float transport.
- `sub_82453AE8`, `generated/reblue_recomp.100.cpp:20298`: water construction
  installs resource vtable0x8208D52C but does not make resource class equivalent
  to a fixed visual technique. Visual technique and entry+248 model technique
  are distinct inputs. The live type is checked below, not inferred from a name.

Admission retains the exact known resource/participant method checks, supported
blend range, native identity and model generation. Known visual types0..13 except
fur1/11 share this setup; type14/unknown types refuse the new visual route. Changed
late inputs/rosters are terminal after effects, never a partially replaced replay.

## Causal reachability correction

Host146/run974 passed the strict mixed cold/reload regression but recorded NO
native water visual scopes. Initial admission had wrongly required visual type8.
Therefore974 is not callback-removal evidence. Its log is retained as the failed
reachability case; no counter threshold or original comparison was disabled.

The correction follows the actual extra1 branches above, adds exhaustive
type0..31/UINT_MAX and blend0..7 CPU admission tests, and keeps ordinary model
admission independent. Host147/run975 reports type mask32: actual visual type5.
At frame900 it already has240 paired native water visual scopes, proving this
connection is reached. Completed cold/reload evidence follows.

## Verification and remaining boundary

377 Python source/scenario checks pass. Deferred CPU build8/PID29988 and
test3/PID22200 exit0 (test0.03s,total0.05s), including the existing lifecycle/
failure tests and new late aliased object-input copy test. Host147/PID38732
exit0: codegen0 writes/deletions and no guest object compilation. Earlier
deferred7/test2 and host146 also built/tested successfully before the reachability
correction; these are not the final tested source.

Unchanged water shaders, binding backend and GPU programs reuse GPU67/water14's
20 two-eye cases, rigid23's55 and snapshot22's8, validation0/0. Those synthetic
pixels do not prove game water/reflection art or stereo. No captures were made.

Remaining: source sorted-entry construction/import and per-entry sampler/world/
alpha/cull exports; source model/primitive association; water parameter descriptors,
image getters and snapshot scheduling; authored bottom/refraction cases and real
game pixel/HDR/stereo sequences. Preserve outgoing state for neighboring legacy
draws until their owners migrate. Next connect water production to the existing
native deferred queue/instance/model owners, retiring source-list entries and
their per-entry dispatch as a bundle, not another callback framework.

The cumulative storage ledger remains
`research/20260906_0333_native-scene-state-bridge.md`. Both runs use180s/800KiB/
192MiB limits with exact owner-profile restoration and captures/perf/dumps off.
The failed974 log remains protected; cleanup/hashes and final evidence follow.

Run975/PID31256/session33818 terminated normally under the supervisor at08:38:22
Eastern,127s after08:36:15 launch. All22 settings took effect;0 raws, exact116B
profile restored (SHA256 `2F1BC38D763A1B7BDBA31F560684FD4AA7E42A714600B8D344F19DA7F38E23B0`).
The full strict mixed/reload chain passes, including neighboring object/material/
light/texture/geometry comparisons, caster/cutout families and deferred effects/
inputs. These are text draw/lifetime checks, not pixels or measured speedup.

- Cold post-event water1560..1860:300 new material begin/end/submission/retirement
  pairs,290 emissions and10 culled. Fresh visual1800..2100 adds300 balanced scopes.
- Reload post-event water3949..4249:300 new pairs,297 emissions and3 culled.
  Fresh visual4200..4500 adds300 balanced scopes. Type mask32 throughout.
- First reloaded water at3049:instance385/generation208,1659 submitted,1658
  retired. All preceding generation94/instance145 packets had retired.
- Last visual sample4500:3109 balanced begins/ends. Last material/draw sample4549:
  3159 balanced direct materials/submissions,3066 emissions,91 culled,3157 retired;
  candidates=consumed3159,unavailable0. Samples are explicitly at different frames.
- Neighboring mixed4200..4500:9791 native deferred submissions/consumptions,
  pending0;5565 legacy draws/11130 material bridges;3386 balanced shared visual
  scopes;300 input batches/refreshes. Rigid generation93->207 and instance144->384
  cold/reload regression passes independently.

No authored bottom/snapshot call was observed. Do not retry this unchanged scene
to claim those cases. The next producer/list change needs its own new observation.

After975 replaced973's strict regression/material evidence, removed13 explicit
superseded files: host145/146, deferred6/7 and test1/2 stdout/stderr, plus hash-
verified run973.538393B logical; immediate free80128286720->80128835584B,548864B
interval gain. Their full text is retired; dated reports/hashes retain outcomes.
Current host147/deferred8/test3,975, causal974, other current fixtures and all
unresolved failures/protected images/raws remain. Builds are reproducible; old
runtime text is not retained elsewhere. No game data or active build tree removed.

Selected retained growth622590B: CPU+45324, EXE/PDB+37376, attachment logs-1556,
runtime replacement/causal evidence+541446. Firstfree80130662400 to post-cleanup
80128835584B:1826816B less free drive-wide; other objects/metadata/Git/system
activity are separate from attributed files or cleanup. No new scoped cache/perf
files. CPU tree78361684B/132files; attachment logs297077B/186files. Next IDs
host148/game976,deferred9/test4; all producers terminal, no allowance reset.

Final host147 hashes:

- EXE48,963,072B: `12300D8A627B2DCB5CDFC3A6B6EBEEE9DD8C20936667A71BEE058E55D0042CB1`.
- PDB111,116,288B: `EC04C8CC9497A155F9E5E15BBADC077EDE34CD94C2DD101CA8D8B2642354AC79`.
- Deferred CPU8: `9F7269A683F18659BB55BDDB5554FB235562DECAE23872A9A6D00EA14BB97D77`.
- Failed reachability974541,432B: `61056EE702A3669E1B9F0C345712615CE4FB90F969C93A69064F5D96A51004B8`.
- Passing975529,878B: `CB34B26A5718B109E73D89A31DDBFE498DA55580C2705F23881A12A55F570657`.
